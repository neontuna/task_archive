<div class="alert alert-info">
  Welcome to the F1 Ticket Archive.  Below is a list of all customers, you can click to view tickets and contacts for that customer.  You can also search for a customer, contact, or ticket notes from the top bar.
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Customer Name</th>
      <th>City</th>
      <th>State</th>
    </tr>
  </thead>
  <tbody>

    <% @customers.each do |c| %>
      <tr>
        <td>
          <%= link_to c.custname, tasks_customer_path(c.id) %> 
        </td>
        <td>
          <%= c.city %> 
        </td>
        <td>
          <%= c.region %> 
        </td> 
      </tr>
    <% end %>

  </tbody>
</table>

<%= will_paginate @customers, renderer: BootstrapPagination::Rails %>